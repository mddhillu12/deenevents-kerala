"use client";

// We are putting the logic directly here to avoid "Module Not Found" errors
import PosterBuilder from "../components/PosterBuilder";

export default function PosterPage() {
  return <PosterBuilder />;
}